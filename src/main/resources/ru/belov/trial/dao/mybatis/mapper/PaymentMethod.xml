<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
        'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>

<mapper namespace="ru.belov.trial.dao.mybatis.mapper.PaymentMethodMapper">

    <insert id="save" parameterType="ru.belov.trial.entity.PaymentMethod">
        INSERT INTO payment_method(uuid, bank_account, active)
        VALUES
        #{uuid},
        #{bankAccount},
        #{active}
    </insert>

    <update id="inactive" parameterType="UUID" >
        UPDATE payment_method SET
        active = false
        WHERE uuid = #{uuid}
    </update>

</mapper>